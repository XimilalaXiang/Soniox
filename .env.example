# 复制本文件为 .env 后再启动 Docker

# 必填：用于签发登录 Cookie 的密钥（请使用长随机字符串）
SECRET_KEY=replace-with-a-long-random-secret

# 可选：设置后会启用固定访问密码（不设置则首次启动时在页面初始化密码）
ACCESS_PASSWORD=

# 可选：镜像标签。默认 latest；可改为 sha-<commit> 或版本号回滚
SONIOX_IMAGE_TAG=latest
